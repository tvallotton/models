use crate::prelude::*;

pub struct Column;

impl Column {
    fn new() -> Self {
        todo!()
    }
}

impl ToTokens for Column {
    fn to_tokens(&self, tokens: &mut proc_macro2::TokenStream) {
        todo!()
    }
}
